现在是 Python 硬编码（world_model_data.py，83 篇论文写死在代码里）。问题很明显：

  1. 每次加论文都要改代码 — 不是数据问题变成了工程问题
  2. 团队没法直接贡献 — Angela/Thomas 想加论文得找你改 Python 文件
  3. 和团队其他数据库无法打通 — entity taxonomy 对齐更困难

  理想链路是：飞书多维表格（团队协作编辑）→ API 同步 → 后端读取 → 前端渲染

  不过这里有个务实的分步策略：

  ┌────────┬──────────────────────────────────────────┬─────────────────────────────────────────────┐
  │  步骤  │                  做什么                  │                    价值                     │
  ├────────┼──────────────────────────────────────────┼─────────────────────────────────────────────┤
  │ Step 1 │ Python 硬编码 → JSON/YAML 文件           │ Claude 和你都能直接编辑数据，不用改代码逻辑 │
  ├────────┼──────────────────────────────────────────┼─────────────────────────────────────────────┤
  │ Step 2 │ JSON → 飞书多维表格 + 同步脚本           │ 团队可以直接在飞书加论文，定期 sync         │
  ├────────┼──────────────────────────────────────────┼─────────────────────────────────────────────┤
  │ Step 3 │ 实时 API 对接（飞书 webhook / 定时拉取） │ 图谱自动刷新                                │
  └────────┴──────────────────────────────────────────┴─────────────────────────────────────────────┘

  Step 1 几乎零成本，马上能做。Step 2 需要飞书 API token + 定义表结构。Step 3 是 P2 的事。

  你想现在推到哪一步？还是先聊聊飞书那边的表结构你已经有了还是要从头建？